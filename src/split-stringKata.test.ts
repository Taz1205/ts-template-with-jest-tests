import { splitString } from "./split-stringKata";

describe("split string kata", () => {
    it("should split a string", () => {
      expect(splitString("")).toBe("");
    });
    // it("should return 5 for add(2,3)", () => {
    //   expect(add(2, 3)).toBe(5);
    // });
  });
  